[project]
name = "vlm-post-training"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "unsloth",
    "torch",
    "trl",
    "datasets",
    "wandb",
    "numpy",
]
